/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [],
    remotePatterns: [],
    unoptimized: false,
    dangerouslyAllowSVG: true,
  },
}

module.exports = nextConfig 